# 版本规则

- 当前版本：**1.1.32**
- 每次改动：patch +1 → 1.0.1、1.0.2、…、1.0.9
- 第 10 次改动：minor +1、patch 归零 → 1.1.0
- 之后同理：1.1.4 → 1.1.5 → … → 1.1.9 → 1.2.0 → …

## 更新日志规则
- **功能更新**（新增功能、更换实现方式、功能增强等）：升版本号后，必须在下方「更新日志」里新增一条，写明版本号和更新内容
- **小改动**（如 UI 位置/排版调整、纯修复等）：只升版本号，不写入更新日志

## 每次改版本时需要同步的位置（index.html / reader.html）
1. index.html / reader.html 标题旁的版本徽章：`v1.1.32`
2. index.html / reader.html 的样式引用（`/src/styles/*.css`）
3. reader.html 的脚本引用（`/src/reader.js`）

---

# 更新日志

## v1.1.29 主页新增每日名言
- 主页底部随机显示一条英语名言 + 中文翻译（内置 12 条学习/坚持主题名言，刷新随机更换）
- 名言以艺术字体展示（英文斜体衬线 + 像素立体投影、中文楷体），复古像素风装饰框，可点 🔄 手动换一句

## v1.1.28 阅读器新增文章导入页
- 主页点击「阅读学习助手」后先进入导入页（不再直接打开示例文章）
- 导入页支持三种方式：拖拽/选择文件上传（.txt/.md/.html）、粘贴文章文本后点「开始阅读」、「加载示例文章」
- 阅读中可点「换一篇」返回导入页重新选择

## v1.1.26 语法分析改为行内下标注（Enpuz 式）
- 抛弃之前的分段标签式输出，改为「整句保持原样、每个词/短语下方小标签标出成分」的展示
- AI 输出结构化 JSON（词块：原文 / 成分 / 所属主句或从句），前端渲染成词块流，主句与从句用不同浅色底区分（悬停可看从句名）
- 顶部保留一行句子摘要（句子类型 / 主干 / 时态语态），下方带成分颜色图例
- AI 输出非 JSON 时自动降级为原文展示，不影响使用

## v1.1.25 语法分析新增结构树 + 原句显示
- 语法分析标签页顶部显示原句并高亮选中单词（与语境翻译一致）
- 新增【结构图】：AI 以树状图输出整句结构（主语/谓语/宾语/状语等，含从句层级），前端渲染成带连接线的图表卡片

## v1.1.24 移除 AI 分析持久缓存
- 移除 localStorage 持久缓存（v1.1.21 引入），AI 语境翻译 / 语法分析仅保留当前页面内存缓存
- 刷新页面后需重新请求 AI，但每次点击同一单词同一句子仍只请求一次（内存缓存）

## v1.1.23 语法分析精简
- 删除语法分析中的「理解要点」结构，输出更聚焦，进一步减少生成时间

## v1.1.22 修复语法分析超时
- 语法分析强制跳过思考模式（全局「思考模式」开关仅对语境翻译生效），大幅缩短生成时间
- 精简语法分析提示词：成分划分只标注核心成分，不再要求逐词展开，减少输出量
- 本地服务器 AI 请求超时从 60 秒放宽到 120 秒，长难句不再轻易超时

## v1.1.21 AI 分析结果持久缓存
- AI 语境翻译 / 语法分析的结果额外存入浏览器本地（localStorage，最多保留最近 300 条）
- 刷新页面或下次打开后，同一单词同一句子直接秒开，不再重复调用 API、不再重复消耗 token

## v1.1.20 语法分析优化
- 删除语法分析中的「中文翻译」结构，输出更聚焦语法本身
- 压缩各分析结构之间的空行间距，展示更紧凑

## v1.1.19 语法分析升级为 Enpuz 风格
- 参考 enpuz.com 的语法分析方式重写「📚 语法分析」提示词，新增：
  - 【句子类型】陈述/疑问/祈使/感叹句 + 简单/并列/复合句
  - 【时态语态】时态与主动/被动
  - 【成分划分】逐词/逐短语标注句子成分（主语/谓语动词/宾语/状语…），多词短语用 ··· 连接，从句按【主句】/【从句】分段并注明从句类型
  - 【主干结构】一句话概括句子主干
  - 【中文翻译】整句通顺翻译
  - 【理解要点】长难句拆分理解与特殊结构说明

## v1.1.13 AI 语境翻译 + 句子语法分析
- 释义弹窗改为标签页：📖 词典（默认，免费快速）｜🤖 语境翻译（AI 结合单词所在句子解释语境含义）｜📚 语法分析（分析句子主干/从句/时态，长难句拆分理解）
- AI 功能使用主页「AI 设置」中绑定的 API Key（自动选择已绑定的服务商），未绑定时点击给出提示
- 同一单词 + 同一句子只调用一次 AI（内存缓存），避免重复消耗 token

## v1.1.11 项目模块化重构
- 前端按功能拆分为 ES Modules：`src/home.js`（主页/AI 设置）、`src/reader.js`（阅读器入口）、`src/reader/`（article 文章、dict 词典、popup 弹窗、tts 发音、sample 示例）、`src/lib/`（dom/api 通用工具）
- 样式拆分：`src/styles/theme.css`（通用）/ `home.css`（主页与设置）/ `reader.css`（阅读器与弹窗）
- 引入 Vite 多页工程化（可选）：`npm install` → `npm run dev` 热更新 / `npm run build` 构建；`python3 server.py --prod` 可服务构建产物
- 顺带修复：思考模式开关未接线、`loadThinking` 未定义（v1.1.9 遗留问题），现在开关真实生效

## v1.1.1 新增主页
- 新增卡片式主页（index.html）：复古像素风格，当前提供「阅读学习助手」入口，后续新功能以卡片形式继续添加
- 阅读器页面移至 reader.html，左上角标题改为「IELTS 学习台」，点击可返回主页

## v1.0.7 例句功能
- 释义弹窗新增「例句」：使用有道双语例句接口，显示英文例句及中文翻译

## v1.0.1 新增发音功能
- 新增单词发音功能：释义弹窗单词右侧加发音按钮（🔊，小字体），用浏览器内置语音合成朗读
